# CLAUDE.md - AI 開發規範

## 開發流程

每次收到需求，嚴格按照以下流程執行：

```
需求 → 開分支 → 規劃 → 開發 → Code Review → 測試 → 請使用者 Review
                                                          │
                                              ┌───────────┤
                                              │           │
                                           同意 ✓      不同意 ✗
                                              │           │
                                        Merge to main   繼續修改
                                                          │
                                                  回到「開發」重跑流程
```

### Step 1: 開分支

- 從 main 建立 feature branch
- 命名規則: `feature/<功能描述>` 或 `fix/<問題描述>`
- 一個需求一個分支，不混做

### Step 2: 規劃需求

- 先分析需求，列出要做的事項（用 TodoWrite 追蹤）
- 說明會動到哪些檔案、影響範圍
- 有疑問先問，不要猜

### Step 3: 開發

程式碼品質三原則：

1. **維護性** - 職責分離，每個模組只做一件事
2. **擴充性** - 新功能不應改動既有程式碼的行為，用擴展不用修改
3. **可讀性** - 命名清楚，結構一致，不寫聰明但難懂的程式碼

### Step 4: Code Review（自我檢查）

提交前自我檢查：

- [ ] 沒有引入安全漏洞（injection、XSS 等）
- [ ] 沒有破壞既有功能
- [ ] 命名一致、結構清楚
- [ ] 沒有多餘的 import、dead code
- [ ] 錯誤處理完整

### Step 5: 測試

- 執行 `python -m pytest tests/ -v`
- **測試失敗時自動重試，至少 3 次**
- 每次重試前要分析失敗原因，修正後再跑
- 不可以把失敗的測試結果丟給使用者，要自己解決
- 全部通過才進到下一步

### Step 6: 使用者 Review

- 將變更摘要報告給使用者
- 使用者同意 → merge to main
- 使用者不同意 → 根據回饋修改 → 回到 Step 3 重跑

## 實作測試規範

AI 在實作測試案例時需要：

1. **提供選項讓使用者確認**
   - 測試的目標模組
   - 測試數量（建議值）
   - 需要 mock 的外部依賴
   - 測試涵蓋的場景（正常/異常/邊界）

2. **自動執行並自我修復**
   - 寫完測試後立即執行
   - 失敗 → 分析原因 → 修正 → 重跑（至少重試 3 次）
   - 3 次都失敗 → 換思路重新設計測試，不要用同一種方法硬撞
   - 全部通過後才報告給使用者

3. **測試品質**
   - 每個測試只測一件事
   - 測試名稱要能看出在測什麼
   - 覆蓋正常路徑、錯誤路徑、邊界條件

## 程式碼規範

### Python

- 遵循 PEP 8
- 使用 type hints（公開 API）
- docstring 用中文
- logging 取代 print
- 常數用大寫 + 底線

### 專案結構

- 新功能放對應的模組目錄
- 不要在一個檔案塞太多功能
- 共用邏輯抽到 utils/

### YAML 定義

- 每個範例檔案頂部加註解說明用途
- 欄位命名用 snake_case

## Git 規範

- Commit message 用中文，簡短描述改了什麼
- 一個 commit 做一件事
- 不要 commit 含有密鑰或敏感資訊的檔案
